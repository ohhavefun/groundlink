# Dependencies and local configuration
node_modules/
.env
.env.*
*.log

# Build output is generated by `bun run build` and published via npm's `files` allowlist.
dist/

# Locally created npm archives
*.tgz
